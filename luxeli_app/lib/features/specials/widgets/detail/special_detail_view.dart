import 'package:flutter/material.dart';

class SpecialDetailView extends StatelessWidget {
  const SpecialDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
