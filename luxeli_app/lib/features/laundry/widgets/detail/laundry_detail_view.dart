import 'package:flutter/material.dart';

class LaundryDetailView extends StatelessWidget {
  const LaundryDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
